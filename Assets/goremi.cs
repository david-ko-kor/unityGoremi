using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class goremi : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("고규형");
        Debug.Log("고규형");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
